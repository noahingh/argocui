/*

Package argo is the manager of views which is relevant to the Argo workflow,
and it is consist of collection(same as `argo list`), sub, follower(`argo logs`) and tree(`argo get`).
Each component, generally, has layout, keybinding and subscribe method for the view.

*/
package argo
